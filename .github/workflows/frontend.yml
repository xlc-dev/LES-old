name: Frontend CI

on:
  pull_request:
    branches:
      - "*"

jobs:
  npm-check:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - run: ls -la
      - run: cd frontend

      - uses: actions/setup-node@v3
        with:
          node-version: 21
          cache: "npm"

      - run: npm ci
      - run: npm run check
